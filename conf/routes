# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /chunker                         controllers.Application.chunker
GET     /redir                           controllers.Application.redir(id: Option[String])